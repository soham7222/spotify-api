CREATE TABLE TRACK_DETAILS (ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY, ISRC  VARCHAR2(100), METADATA CLOB CONSTRAINT ensure_json CHECK (METADATA IS JSON), CONSTRAINT isrc_unique UNIQUE (ISRC));
